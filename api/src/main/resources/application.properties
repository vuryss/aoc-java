# Logs exceptions during REST request parameter handling
quarkus.log.category."org.jboss.resteasy.reactive.server.handlers.ParameterHandler".level=DEBUG
quarkus.log.category."WebApplicationException".level=DEBUG

# Path to the CLI uber-jar (override per environment if needed)
aoc.cli.jar.path=${JAVA_APP_DIR}/cli.jar